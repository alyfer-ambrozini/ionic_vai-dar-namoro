import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vai-da-namoro-ta',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
